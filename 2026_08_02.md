# Pind 작업 일지 — 2026-08-02

**작성**: 오늘 세션 정리 (포트폴리오/회고용, 타임라인 순)
**브랜치**: `feat/phase-3-4` (미커밋)

> 오늘 한 일을 시간 순서대로 정리한다. 뭘 하려다 안 돼서 뭘로 바꿨는지, 어떤 버그를 어떻게 잡았는지 위주로 남긴다.

---

## 배경: 왜 이 작업을 시작했나

`~/pind` 아래에 `Bin_pind`(실제 동작하는 백엔드+프런트, Phase 0~4 완료)와 별개로 `Create UI Prototype`이라는 Figma Make 산출물이 있었다. Vite+React로 만든 2100줄짜리 단일 `App.tsx`에 화면 8개(피드, 영상 상세, 프로필, 분석, 크리에이터 대시보드 등)가 다 목업으로 구현돼 있었고, 첨부된 기획 문서(`pind-frontend-brief.md`, `pind-user-features.md`)에 "홈을 URL 입력에서 분석된 영상 피드로 바꾼다", "로그인 없이도 둘러볼 수 있게 연다", "크리에이터/일반 사용자 UI를 분리한다" 같은 제품 방향이 적혀 있었다.

목표: 이 프로토타입의 UI/UX를 가져오되, Bin_pind의 실제 파이프라인·DB·인증에 붙여서 진짜로 동작하게 만드는 것.

## 1단계: 조사

- `Create UI Prototype`의 "지도"(`MockMap`)는 실제 좌표가 아니라 index 기반 퍼센트 좌표로 찍는 가짜였다. 로그인도 가짜(로컬 state만). 저장/컬렉션/구독도 전부 메모리에만 존재.
- `App.tsx`가 `components/ui/*`(shadcn 48종 스캐폴딩)를 단 한 번도 import하지 않는다는 걸 grep으로 확인 — 전부 죽은 코드였다. 그래서 그 폴더는 통째로 무시하고 화면 함수의 JSX/로직만 옮기기로 했다.
- 브리핑 문서 자체가 "구독 결제·인구통계 추천·크리에이터 상세지표는 지금은 틀만"이라고 명시하고 있어서, 이 부분은 굳이 실제로 안 만들어도 되는 근거가 됐다.

## 2단계: 범위 합의

두 가지를 사용자에게 먼저 물어봤다.
1. **어디까지 실제로 동작시킬까** → "MVP만 실제 작동, 나머지는 UI만" 으로 정함
2. **RLS를 바꿔서 완료된 영상을 공개할까** → "바꾼다"로 정함 (기존엔 본인 것만 보이는 private 정책이었음)

이 두 가지 답을 기준으로 계획을 세우고 승인받은 뒤 구현 시작.

## 3단계: 백엔드 구현

- **Supabase 마이그레이션**: RLS를 "본인 것 + 완료된 건 공개"로 교체, 신규 테이블 5개(`saved_places`, `saved_videos`, `collections`, `collection_places`, `place_events`) 추가.
  - **막힌 점**: `supabase` CLI도, `psql`도 이 컴퓨터에 안 깔려 있었음.
  - **해결**: 이미 설치돼 있던 `psycopg2`로 SQL 파일을 직접 실행하는 짧은 파이썬 스크립트를 만들어서 적용.
- **Video 테이블에 컬럼 4개 추가** (`region`, `theme`, `creator_name`, `thumbnail_url`) + Alembic 마이그레이션.
- **파이프라인 수정**:
  - `download.py`가 yt-dlp 메타데이터에서 `uploader`/`thumbnail`을 이미 받아오고 있었는데 그냥 버리고 있었음 → 저장하도록 수정.
  - `analyze_video.py`의 Gemini 프롬프트를 "장소 배열만" 반환하던 것에서 `{region, theme, places}` 객체로 확장. 기존 테스트(`_parse_candidates`)가 배열 파싱만 검증하고 있어서, 정규식이 객체 안에 있어도 첫 `[...]`를 그대로 찾아내는 걸 확인하고 그 함수는 안 건드림 — region/theme만 별도의 가벼운 정규식으로 뽑는 함수를 추가해서 **기존 테스트 5개를 안 깨고** 확장.
- **`get_current_user` 최초 구현**: 크리에이터 대시보드 통계(본인 영상만 조회 가능해야 함)를 만들다 보니, 루트 문서에 "JWKS 검증"이라고 적혀만 있고 실제로는 없던 JWT 인증을 이번에 처음 구현(`SUPABASE_JWT_SECRET` 기반 HS256 디코드). `pyjwt` 의존성 추가.
- **`GET /api/v1/creator/videos/{id}/stats`** 신규 라우터: 본인 영상만, `place_events`를 place별/타입별로 집계.
- **`GET /api/v1/places?ids=`** 파라미터 추가: 여러 영상에 걸친 "내 지도" 조회용.
- **검증 중 발견**: 내가 만든 스키마 변경 때문에 기존 pytest 1개(`test_schemas.py`)가 깨짐 → 테스트 데이터에 새 필드 4개 추가해서 고침. `ruff`/`mypy`/`pytest` 돌려서 내가 만든 새 파일들은 전부 클린한 걸 확인(기존 `google-genai` 타입 이슈는 손 안 댐, 무관한 pre-existing 이슈).

## 4단계: 프런트엔드 구현

Vite 프로토타입을 그대로 안 쓰고, 화면별 로직만 뽑아서 Next.js `apps/web`에 새로 작성(지난 세션의 `pind_shadcn_map-main` 이식과 같은 방식). 지도는 프로토타입의 가짜 `MockMap` 대신 이미 있던 실제 Leaflet `Map.tsx`를 재사용하되, 장소 타입별 색깔 핀을 추가로 지원하도록 확장.

- 신규 훅 7개: `useFeed`, `useVideo`, `useMyMaps`, `useSavedVideos`, `usePlaceEvents`, `useCreatorStats`, `usePlacesByIds`
- 신규 페이지 5개: 홈(피드), 영상 상세, 프로필, 분석, 크리에이터 대시보드
- 장소 타입(카페/맛집/명소/숙소/체험)은 백엔드 스키마를 안 건드리고 `category` 자유 텍스트를 클라이언트에서 키워드로 매핑(`lib/placeType.ts`)
- 로그인 모달은 따로 안 만들고 기존에 잘 되어있던 `/login` 페이지로 그대로 연결(중복 구현 안 함)
- Playwright로 실제 스크린샷 검증: 피드에 실제 영상 13개·장소 59개, 영상 상세에 실제 도쿄 지도+9개 핀, 보호된 페이지 리다이렉트까지 확인. `make verify-js` 전체 통과.

## 5단계: 사용자 테스트 중 발견된 버그들

여기서부터는 실제로 서버를 띄우고 써보면서 하나씩 터진 것들.

### 버그 1 — 장소 클릭 시 구글맵 연결이 사라짐
- **증상**: 예전 버전에선 장소를 누르면 구글맵 상세 페이지로 넘어갔는데, 새로 만든 영상 상세 화면에선 그 기능이 없었음.
- **원인**: 오늘 새로 만든 `/videos/[id]` 페이지는 지난 세션에 만들어둔 구글맵 연결 로직(`CheckedVideosPanel.tsx`)을 안 가져왔던 것. 단순 누락.
- **조치**: 구글맵 URL 생성 함수를 `lib/googleMaps.ts`로 공용화하고, 영상 상세·내 지도(`MyMapPanel`) 양쪽에 외부 링크 아이콘으로 추가. 클릭 시 "action" 이벤트도 같이 기록해서 크리에이터 대시보드 퍼널에 반영되게 함.

### 요청 — 예전 영상들의 썸네일이 안 보임
- **원인**: 오늘 스키마에 추가한 `thumbnail_url`은 컬럼 추가 *이전에* 분석된 영상들은 당연히 `null`.
- **해결**: YouTube는 영상 ID만 있으면 항상 예측 가능한 공개 썸네일 URL(`img.youtube.com/vi/{id}/...`)을 제공한다는 점을 이용해서, 저장된 `thumbnail_url`이 없으면 영상 URL에서 즉석으로 유도하는 `lib/youtube.ts`를 추가. API 키도 필요 없고, 예전 영상까지 전부 커버됨.

### 논의 — 장소 상세를 우리 페이지 안에서 사진/별점까지 보여줄 수 있는가
- 가능은 하지만 Google Places "Place Details" + "Photo" API를 추가로 호출해야 하고(비용 발생, 캐싱 전략 필요) 새 UI 패널도 만들어야 하는 큰 작업이라, 사용자와 상의 후 **지금은 보류**하고 `PROGRESS.md`에 v2 후보로만 기록.

### 버그 2 — "내 영상 분석하기"를 누르면 계속 로그인 화면만 뜸
- **증상**: 로그인된 상태인데도 `/analyze`, `/profile`, `/creator/dashboard`로 이동하면 자꾸 `/login`으로 튕김.
- **원인**: `useAuth()`는 Supabase 세션 확인이 비동기라서 첫 렌더 시점엔 항상 `user = null`(진짜 로그인 여부와 무관하게)이고, 그걸 확인 다 하면 `loading = false`가 됨. 그런데 세 페이지 모두 `loading`은 안 보고 바로 `if (!user) router.push("/login")`을 실행해버려서, 로그인된 사용자도 페이지 마운트 첫 틱에 무조건 튕겨나가고 있었음. 페이지 이동할 때마다 100% 재현되는 구조적 버그였음.
- **해결**: 세 페이지 다 `loading`을 먼저 기다리는 가드를 추가(`loading` 중엔 "불러오는 중..." 표시, 끝난 뒤에만 `user` 여부 판단).
- **검증**: Playwright로 실제 회원가입 → `/analyze` 이동까지 재현해서, 고치기 전엔 `/login`으로 튕기고 고친 후엔 `/analyze`에 정상적으로 머무는 걸 직접 확인.

### 버그 3 — 영상 링크를 넣고 돌리면 "cannot add postgres_changes callbacks ... after subscribe()" 에러
- **증상**: `/analyze`에서 실제로 링크 넣고 제출하면 런타임 에러 발생.
- **원인**: 예전에 한 번 겪었던 것과 같은 종류의 버그. `AnalyzePage`가 `useVideoStatus(latestVideoId)`를 직접 호출하고, 그 안에 렌더링되는 `<VideoForm />`도 내부적으로 똑같이 `useVideoStatus(latestVideoId)`를 호출하고 있어서, 같은 이름의 Supabase Realtime 채널을 두 곳에서 동시에 구독하려다 충돌.
- **해결**: `VideoForm`이 상태 변화를 `onStatusChange` 콜백으로 부모에 알려주도록 바꾸고, `AnalyzePage`는 자기가 직접 구독하는 대신 이 콜백만 받도록 수정 — 구독 지점을 한 군데로 통일.
- **검증**: 회원가입 → `/analyze`에서 실제 유튜브 링크 제출까지 재현, 에러 없음 확인 + API 로그에서 webhook 수신·다운로드·Gemini 업로드까지 실제로 파이프라인이 도는 것 확인.

## 6단계: 추가 요청 — 분석 후 진행 상황을 홈 화면에서 보여주기

사용자 피드백: "링크 넣는 화면에 계속 있어봐야 할 게 없다, 분석 누르면 홈으로 돌아가서 상단에 작게 진행상황(추출중/완료)이 떴으면 좋겠다."

- `VideoForm`에 `onSubmitted` 콜백을 추가해서, 제출 성공 시 `/analyze` 페이지가 홈(`/`)으로 이동하도록 함.
- 홈 페이지(피드)에 `useVideoStatus`를 새로 구독해서 Navbar 바로 아래에 작은 상태 배너("장소 추출 중...", "완료! 지도에서 장소를 확인하세요." 등)를 표시. 홈 페이지는 `VideoForm`을 렌더링하지 않으므로 이중 구독 위험 없이 안전하게 추가 가능했음.
- 상태 라벨/색상 매핑이 `VideoForm.tsx`와 홈 페이지 두 곳에서 필요해져서 `lib/pipelineLabels.ts`로 공용화(중복 제거).
- Playwright로 회원가입 → 제출 → 홈 이동 → 배너 표시(`장소 추출 중...`)까지 실제 스크린샷으로 검증.

---

## 오늘 검증에 쓴 테스트 계정/데이터

Playwright로 여러 번 회원가입 테스트를 하면서 임시 계정(`pind-test-*@example.com`) 여러 개와 테스트용 영상(Rick Astley, Me at the zoo 등) 몇 개를 실제로 만들었는데, 검증 끝난 뒤 Supabase admin API/DB로 전부 삭제해서 실제 데이터엔 안 남아있음.

## 오늘 만든/고친 파일 (대략)

**백엔드**: `models/video.py`, `models/place_event.py`(신규), `schemas/video.py`, `pipeline/{types,download,analyze_video,orchestrator}.py`, `routers/{places,creator(신규)}.py`, `deps.py`, `exceptions.py`, `main.py`, Alembic 마이그레이션 신규 1개, Supabase 마이그레이션 신규 1개

**프런트엔드**: `lib/{dto,placeType,googleMaps,youtube,pipelineLabels}.ts`, `components/{Map,MapWrapper,VideoForm,Navbar,ShareModal,UpgradeModal,CreatorVerifyModal,CancelSubModal,MyMapPanel}.tsx`, `hooks/{useFeed,useVideo,useMyMaps,useSavedVideos,usePlaceEvents,useCreatorStats,usePlacesByIds}.ts`, `app/page.tsx`, `app/videos/[id]/page.tsx`, `app/profile/page.tsx`, `app/analyze/page.tsx`, `app/creator/dashboard/page.tsx`

## 다음에 볼 사람을 위한 요약

오늘 한 일은 결국 "예쁜 목업 하나를 진짜로 동작하는 기능으로 바꾸는 과정"이었고, 그 과정에서 만난 버그 3개(구글맵 링크 누락, 로그인 상태 레이스 컨디션, Realtime 채널 중복 구독)는 전부 "화면을 실제로 켜서 써보지 않았으면 못 찾았을" 종류였다. 코드만 보고 통과되는 typecheck/lint/pytest와, 실제로 로그인해서 버튼을 눌러보는 것 사이의 간극이 컸던 하루.
