# Pind 작업 일지 — 2026-07-01

**작성**: 오늘 세션 정리 (팀원 공유용)
**브랜치**: `feat/phase-3-4` (아직 커밋 전, 로컬 working tree에만 존재)

> 이 문서는 오늘 하루 동안 진행한 프런트엔드 이식 작업 + 로컬 환경에서 실제로 영상 분석이 끝까지 도는지 검증하며 발견/수정한 이슈들을 시간 순으로 정리한 기록입니다. `PROGRESS.md`의 정식 ADR/체크리스트에는 요약만 반영되어 있고, 세부 디버깅 과정은 여기 남깁니다.

---

## 오늘 한 일 요약

1. 참고 리포 2개(`FinalProject-master`, `pind_shadcn_map-main`) 분석 후 `pind_shadcn_map-main`의 프런트엔드 UI를 Bin_pind `apps/web`에 이식
2. 로컬 실행 검증 중 발견된 백엔드 버그 4건 수정 (Places API deprecated 엔드포인트, shapely 누락, ffmpeg 누락, geocode 에러 핸들링)
3. 프런트엔드 렌더링 버그 2건 수정 (CSS stacking으로 인한 UI 미표시, Realtime 채널 중복 구독 크래시)
4. `.env.example`에 실수로 커밋될 뻔한 실제 API 시크릿 원복 (플레이스홀더로)

---

## 1. 리포 조사 및 방향 결정

- `FinalProject-master`: 프런트엔드 없음, FastAPI 백엔드 전용(YouTube transcript + Gemini 텍스트 분석, Celery 기반). Bin_pind의 Phase 3+4가 이미 더 발전된 방식(Gemini Files API로 영상 자체 업로드 분석)으로 구현되어 있어 **이식 대상에서 제외**.
- `pind_shadcn_map-main`: Next.js 15 + shadcn/ui 목업. 히스토리 사이드바, 체크된 영상 패널, 로그인 화면 UI는 재사용 가치 있음. 단, 지도는 실제 Google Maps 렌더링이 아니라 **가짜 placeholder**(그라디언트 배경 + index 기반 pin)였고, `lib/google-maps-api.ts`는 Google My Maps 내보내기용 죽은 코드였음. Google 로그인 UI도 있었으나 Bin_pind ADR(Supabase 이메일/비밀번호 유지)에 따라 이식 안 함.
- 결정: 프런트엔드 UI만 이식, 지도는 Bin_pind의 기존 실제 Leaflet `Map.tsx`(YouTube 팝업 포함) 그대로 사용. 백엔드는 이번 세션 범위 밖.

## 2. 프런트엔드 이식 (`apps/web`)

### 신규 파일
- `components/ui/{card,checkbox,input,label,scroll-area,separator}.tsx` — shadcn 프리미티브 6종 이식 (next/* 의존 없음, 기존 `cn` 헬퍼 그대로 호환)
- `hooks/useVideos.ts` — Supabase `videos` 테이블 직접 조회 (백엔드에 `/api/v1/videos` 목록 엔드포인트가 없어서 프런트에서 직접 조회하는 방식 채택)
- `hooks/usePlacesByVideo.ts` — `usePlacesByVideo`(단일) / `usePlacesByVideos`(여러 개 병렬, `useQueries` 기반). 기존 `Map.tsx`에 있던 쿼리 로직을 공용 훅으로 추출
- `components/HistorySidebar.tsx` — 영상 히스토리 목록 (체크박스, 상태 아이콘 썸네일, 영상별 실제 장소 개수 표시)
- `components/CheckedVideosPanel.tsx` — 체크된 영상들의 장소를 그룹으로 표시. **원본의 Google Maps 내보내기 기능은 제거** (Google OAuth 필요+ ADR 위반이라 드롭). 각 장소 클릭 시 `google_place_id` 기반으로 구글맵 상세 페이지 새 탭 오픈
- `components/MobileOverlay.tsx` — 모바일 히스토리 드로어 (원본의 "장소 상세 패널" 서브뷰는 실제 목업에서도 안 쓰이던 죽은 기능이라 제외)
- `components/PindDashboard.tsx` — 데스크톱(사이드바+지도)/모바일(풀스크린 지도+햄버거 메뉴) 레이아웃 조립

### 수정 파일
- `components/Map.tsx` — `videoIds?: string[]` prop 추가. 체크된 영상이 있으면 해당 영상들의 장소만 병합 표시, 없으면(기본값) 전체 장소 표시하도록 fallback 로직 추가
- `components/MapWrapper.tsx` — `videoId`/`videoIds` prop 전달하도록 확장
- `components/VideoForm.tsx` — 사이드바 내장 폼 → 지도 위에 뜨는 알약형 검색바(링크 아이콘 + placeholder + 원형 검색 버튼) 로 재스타일링. **데스크톱/모바일 공용으로 반드시 1개만 마운트**해야 함 (아래 3-2 버그 참고)
- `app/places/page.tsx` — 기존 단순 지도 페이지 → `PindDashboard`로 교체 (CLAUDE.md 문서상 "지도 + 장소 목록" 역할)
- `app/(auth)/login/page.tsx` — 기존 Supabase 이메일/비밀번호 로직은 그대로, Card+그라디언트 배경으로 비주얼만 개선
- `package.json` — `@radix-ui/react-{checkbox,label,scroll-area,separator}` 추가

### UI 반영 (팀원 스크린샷 요청 기반, 2차 수정)
사용자가 참고 화면 3장을 공유하며 요구한 사항 반영:
1. 영상 링크 입력창을 사이드바가 아니라 **지도 위 상단 중앙 플로팅 검색바**로
2. 히스토리 목록에 **영상별 실제 장소 개수** 표시 (`usePlacesByVideos`로 전체 영상 일괄 조회)
3. **체크박스 체크 → 자동으로 "Checked Videos" 패널 오픈** (기존엔 별도 클릭 액션이 있었으나 단순화)
4. 패널의 장소 클릭 → **구글맵 상세 페이지로 이동** (커스텀 상세 카드 대신)

## 3. 로컬 실행 중 발견/수정한 버그

### 3-1. 백엔드

| 버그 | 원인 | 조치 |
|---|---|---|
| `GET /api/v1/places` 지오코딩 100% 실패 | `resolve.py`가 Google **Legacy Places API**(`findplacefromtext`) 호출 중인데 프로젝트에 New API만 활성화됨. 게다가 응답의 `status`/`error_message`를 안 보고 무조건 "not_found" 처리 | `resolve.py`를 **Places API (New)** `POST /v1/places:searchText`로 마이그레이션. 에러 응답도 파싱해서 로그에 실제 원인 노출하도록 수정. (Google Cloud Console에서 API 키의 "API restrictions"에 Places API (New) 허용 추가는 사용자가 처리) |
| `GET /api/v1/places` 500 에러 | `geoalchemy2`가 PostGIS geometry → lat/lng 변환에 필요한 `shapely`가 venv에 미설치 | `pyproject.toml`의 `geoalchemy2>=0.15.0` → `geoalchemy2[shapely]>=0.15.0`로 수정, 재설치 |
| Gemini 파일 업로드 실패 (`FileState.FAILED`, code 13) | **ffmpeg 미설치** → yt-dlp가 `bestvideo+bestaudio`를 못 합치고 오디오 전용 스트림(`.f251.webm`)만 다운로드 → Gemini가 "영상 없음"으로 처리 거부 | `conda install -n base -c conda-forge ffmpeg -y`로 설치. 직접 다운로드 테스트로 video(AV1)+audio 스트림 정상 포함 확인 |
| Alembic 마이그레이션 lint 에러 | `sqlalchemy` 미사용 import (기존 미커밋 WIP 파일) | import 제거 |
| `resolve.py`의 stale mypy `# type: ignore` | 관련 없는 기존 코드, 검증 중 같이 발견 | 제거 |

추가로 **Supabase 프로젝트 DNS 일시적 미해석** 현상이 한 번 있었으나(로그인/DB 연결 모두 실패), 재확인 시 정상 동작 — 원인 불명(일시적 이슈로 추정), 이후 재발 없음.

### 3-2. 프런트엔드

| 버그 | 원인 | 조치 |
|---|---|---|
| 상단 검색바가 화면에 아예 안 보임 | **CSS stacking context 버그**. `.leaflet-container`가 자체 stacking context를 안 만들어서 Leaflet 내부 레이어(z-index 200~800)가 검색바(z-10)를 그냥 뚫고 위로 올라옴 | 검색바/모바일 메뉴 버튼/모바일 오버레이의 z-index를 전부 `z-[1000]`으로 상향. Playwright로 실제 스크린샷 찍어서 시각적으로 확인 |
| `Unhandled Runtime Error: cannot add postgres_changes callbacks ... after subscribe()` | 검색바(`VideoForm`, Supabase Realtime 구독 포함)를 데스크톱용/모바일용으로 각각 렌더링했는데, `hidden`/`md:hidden`은 **CSS로만 숨길 뿐 DOM에는 둘 다 마운트됨** → 동일 채널을 이중 구독 | `VideoForm`을 화면 전체에 **딱 1개만** 마운트하도록 `PindDashboard` 구조 변경 (반응형 `left` 오프셋으로 위치만 조정) |
| 체크박스 아무것도 선택 안 하면 지도가 비어 보임 | `Map`에 빈 `videoIds=[]`를 넘기면 아무 장소도 안 보여주도록 되어 있었음 | 선택된 영상이 없으면 전체 장소를 기본으로 보여주도록 fallback 추가 |

### 3-3. 보안

- `apps/api/.env.example`, `apps/web/.env.example`(둘 다 git 추적 파일)에 플레이스홀더 대신 **실제 Supabase service-role key, JWT secret, Gemini API key, Google Places API key**가 들어가 있던 것을 발견 → `git checkout`으로 플레이스홀더 원복 (커밋 전이라 히스토리엔 안 남음). **참고**: 실키 자체는 이미 로컬에 평문으로 있었으니, 다른 곳에 공유된 적 있다면 로테이션 권장.

---

## 검증 상태

- `apps/web`: `pnpm typecheck`, `pnpm lint` 통과
- 루트: `make verify-js` 통과. `make verify-api`는 이번 세션과 무관한 기존 파이프라인 파일(`download.py`, `orchestrator.py`)의 포맷팅 이슈가 남아있어 미통과 (범위 밖으로 보류)
- 로컬 dev 서버(API `:8000` + Web `:3000`)로 `/`, `/places`, `/login` 라우트 전부 확인
- YouTube 링크 실제 제출 → 다운로드 → Gemini 분석 → 지오코딩 → 지도 마커 표시까지 **엔드투엔드 파이프라인 성공 확인** (ffmpeg 설치 후)

## 아직 안 된 것 / 다음 세션 후보

- 오늘 변경사항 전부 **미커밋 상태** (`feat/phase-3-4` 브랜치 working tree). 커밋 여부/단위는 팀 논의 필요
- 백엔드(FinalProject-master 로직) 이식 — 미착수
- `make verify-api` 통과시키려면 `download.py`/`orchestrator.py` 포맷팅 정리 필요
- `VideoDTO`에 썸네일 필드 없어서 히스토리 목록에 실제 유튜브 썸네일 대신 상태 아이콘 placeholder 사용 중 — 백엔드에 `thumbnail_url` 컬럼 추가 시 개선 가능
- `make gen:types` 미실행 — `lib/dto.ts` 임시 타입 계속 사용 중 (기존부터 알려진 이슈)
- ffmpeg는 이번에 **로컬 환경(conda base)에만** 설치함 — 다른 팀원 로컬/배포 환경에도 별도 설치 필요 (README/설정 가이드에 추가 권장)

---

## 의사결정 로그 (ADR)

| 날짜 | 결정 | 비고 |
|------|------|------|
| 2026-07-01 | 프런트엔드는 `pind_shadcn_map-main` UI 이식, 백엔드는 `FinalProject-master` 미이식 | Bin_pind Phase 3+4가 이미 Gemini Files API 기반으로 FinalProject-master(transcript+Celery 방식)보다 발전된 파이프라인 보유. FinalProject-master는 프런트엔드 자체가 없음(백엔드 전용 리포) |
| 2026-07-01 | `pind_shadcn_map-main`의 Google 로그인 UI, Google Maps 내보내기(My Maps/KML) 기능은 이식하지 않음 | 기존 ADR(Supabase 이메일/비밀번호 인증만 사용)과 충돌. Google Maps 내보내기는 OAuth 필요 + 핵심 기능 아님 → 드롭 |
| 2026-07-01 | 지도 렌더링은 Leaflet+OSM 유지, `pind_shadcn_map-main`의 지도 UI는 이식 안 함 | 원본 목업의 "지도"는 실제 Google Maps 연동이 아니라 index 기반 좌표의 가짜 placeholder였음. Bin_pind는 이미 실좌표+YouTube 팝업 포함한 실제 Leaflet 지도(`Map.tsx`)를 보유 |
| 2026-07-01 | 영상 히스토리 목록(`useVideos`)은 FastAPI가 아니라 Supabase 클라이언트로 직접 조회 | `/api/v1/videos` 목록 엔드포인트가 백엔드에 없음. 신규 엔드포인트를 만드는 대신, 기존 `VideoForm`의 "Supabase 직접 INSERT" 패턴과 동일한 방식(직접 SELECT)으로 통일 |
| 2026-07-01 | 체크된 여러 영상의 장소는 `/api/v1/places?video_id=`를 영상별로 병렬 호출해 프런트에서 병합 (`usePlacesByVideos`, TanStack Query `useQueries`) | 백엔드에 다중 video_id 필터/집계 엔드포인트가 없음. 이번 세션 범위를 프런트엔드로 한정했기 때문에 백엔드 변경 없이 해결. 체크 수가 많아지면 N+1 호출 비용 재검토 필요(알려진 이슈로 기록) |
| 2026-07-01 | `CheckedVideosPanel`의 장소 클릭 시 커스텀 상세 카드 대신 구글맵 상세 페이지로 이동 (`https://www.google.com/maps/place/?q=place_id:{google_place_id}`) | 원본의 `PlaceDetailsPanel`(플로팅 카드)은 원본 목업에서도 실제로는 안 쓰이던 미완성 기능이었음. Bin_pind `Map.tsx`가 이미 Leaflet 네이티브 팝업(YouTube 임베드 포함)으로 장소 상세를 보여주고 있어 중복 UX를 만들지 않기로 결정 |
| 2026-07-01 | `resolve.py`: Google Places API를 Legacy(`findplacefromtext`, GET)에서 New(`places:searchText`, POST)로 마이그레이션 | 프로젝트에 Legacy Places API가 비활성화되어 지오코딩이 100% 실패(`REQUEST_DENIED`)했음. Google이 Legacy를 사용 중단 방향으로 안내하고 있어 콘솔 설정 대신 코드 마이그레이션을 택함. 응답의 `status`/`error_message`를 파싱해 로그에 실제 원인을 남기도록 에러 핸들링도 개선(기존엔 무조건 "not_found"로만 기록됨) |
| 2026-07-01 | `apps/api/pyproject.toml`: `geoalchemy2>=0.15.0` → `geoalchemy2[shapely]>=0.15.0` | PostGIS geometry ↔ lat/lng 변환(`to_shape`)에 `shapely`가 필수인데 optional extra라 venv에 누락되어 있었음. `/api/v1/places`가 500 에러를 내던 직접 원인 |
| 2026-07-01 | 로컬 개발 환경에 `ffmpeg`를 `conda install -n base -c conda-forge ffmpeg`로 설치 | yt-dlp가 `bestvideo+bestaudio`를 mp4로 병합하려면 ffmpeg 필요. 없으면 오디오 전용 스트림(`.f251.webm`)만 받아져 Gemini Files API가 "영상 없음"으로 처리 실패(`FileState.FAILED`, code 13) |
| 2026-07-01 | 지도 위 플로팅 UI(검색바, 모바일 메뉴, 모바일 오버레이)의 z-index를 `z-10`/`z-50` → `z-[1000]`으로 상향 | `.leaflet-container`가 자체 stacking context를 생성하지 않아(z-index:auto), 내부 Leaflet 레이어(타일 200, 마커 600, 컨트롤 800)가 stacking 비교에서 그대로 노출되어 낮은 z-index의 형제 요소를 덮는 CSS 버그. Playwright로 실제 렌더링 스크린샷 찍어 원인 확정 후 수정 |
| 2026-07-01 | `VideoForm`(검색바)은 데스크톱/모바일 레이아웃 통틀어 페이지당 정확히 1개만 마운트 | Tailwind `hidden`/`md:hidden`은 CSS로만 화면 전환할 뿐 DOM에는 두 인스턴스가 동시에 존재함. `VideoForm`이 `useVideoStatus`로 Supabase Realtime 채널을 구독하는데, 두 인스턴스가 동일 채널을 이중 구독하면서 `cannot add postgres_changes callbacks ... after subscribe()` 런타임 에러 발생 → 반응형 위치 조정만으로 해결하고 컴포넌트 자체는 단일 인스턴스로 통합 |
| 2026-07-01 | `Map`: 체크된 영상이 하나도 없으면 필터 없이 전체 장소를 기본 표시 | `videoIds=[]`를 그대로 필터로 써서 아무 장소도 안 보여주던 걸, "아무것도 선택 안 함 = 전체 보기"로 기본값 변경. Phase 4 때 있던 기존 단일-영상 fallback 동작과 일관성 유지 |
| 2026-07-01 | `.env.example`(tracked) 파일은 반드시 플레이스홀더만 유지, 실제 시크릿은 `.env`/`.env.local`(gitignored)에만 | 작업 중 `apps/api/.env.example`, `apps/web/.env.example`에 실제 Supabase service-role key/JWT secret/Gemini·Places API key가 들어가 있던 것을 발견. 커밋되기 전에 `git checkout`으로 원복 |
